{   //all of it is a compound_statement -> declarations and statements

    var Tensor a[2][2]; // var, tensor -> declaration_type, rest is declarator
    var Tensor b[2][2] = [[3 ,4], [5, 6]]; //declarator = initalizer
    cns int c = 16; 
 /* if(  c > 0 ){ //selection statement
        b = c*a ;
    }
    elif(c == 0){
        b = (c + 1)*a; 
    }

    loop(int i = 0; i < 10 ; i++){
        // by default cns
        b  = b + b*c; //assign_exp
    } */
/*
    backward(b); //expression ; -> expression_statemnt-> statement(on the top)
    print(grad(a)); // lib_funcs(unary_exp) */
}
